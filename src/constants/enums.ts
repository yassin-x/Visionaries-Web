export enum Directions {
  LTR = "ltr",
}

export enum Languages {
  EN = "en",
}

export enum Routes {
  ROOT = "/",
  ABOUT = "about",
  OURSERVICES = "our-services",
  FAQ = "faq",
}

export enum Environments {
  PROD = "production",
  DEV = "development",
}
